apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: tpregisteret
  namespace: default
  labels:
    team: teampeon
spec:
  image: repo.adeo.no:5443/tpregisteret:latest
  port: 8080
  team: teampeon
  liveness:
    path: isAlive
  readiness:
    path: isReady
  secrets: true
  replicas:
    min: 1
    max: 2
  resources:
    limits:
      cpu: 1000m
      memory: 512Mi
    requests:
      cpu: 200m
      memory: 256Mi